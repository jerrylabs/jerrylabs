---
import Layout from '../layouts/Layout.astro'
import Jumbotron from '../components/Jumbotron.astro'
import webs from '../data/weby.json'
---
<Layout>
  <Jumbotron title="Weby od JerryLabs" className="jumbotron__matrix" image="matrix" />

    <div class="container">
      <p class="big center pretty">
        Kromě her se zabýváme rovněž programováním a webdesignem.
        <br>
        Pro statické webové stránky poskytujeme hosting zdarma.
        <br>
        Dvojklikem vstupte do Matrixu.
      </p>
    </div>
    <div class="container cards">
      {webs.map((web, i) => (
        <a
          class="card card--vertical card--third card--centered"
          data-attribute="SRL" href={web.image} title={web.title}
          data-fslightbox="webs-lightbox"
        >
          <img class="card__image" src={web.image} alt={`${web.title} (${web.year})`} />
          <div class="card__content">
            <h3 class="card__title">{web.title}</h3>
            {/* <div class="card__description">{web.year}</div> */}
          </div>
        </a>
      ))}
    </div>
  <section>
    <div class="center">
      Potřebujete vyrobit či zprovoznit web? Neváhejte nás <a href="/kontakt">kontaktovat</a>!
    </div>
  </section>
</Layout>

<script src="../scripts/fslightbox.js"></script>